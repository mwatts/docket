{
  "body": "## Goal\n\nWhen `docket done <bug>` is run from trunk, it should auto-detect and switch to that bug's workspace to perform the full workspace workflow.\n\n## Acceptance Criteria\n\n- [x] Detect if bug has a workspace at `../ws-<bug-id>`\n- [x] If workspace exists, cd to it before running workspace workflow\n- [x] Run the full workspace workflow (snapshot, describe, link, sync)\n- [x] Return to original directory after completion\n- [x] If no workspace exists, behave as current (just mark done)\n\n## Context\n\nCurrent pain point:\n1. `docket work <bug>` - starts claude in workspace\n2. Quit claude - back in trunk (exec behavior)\n3. Must manually `cd ../ws-<bug>`\n4. Run `docket done <bug>`\n5. Must manually `cd` back\n\nImproved flow:\n1. `docket work <bug>` - starts claude in workspace\n2. Quit claude - back in trunk\n3. `docket done <bug>` - auto-finds workspace, does everything\n4. Done!\n\n## Log\n- 2026-01-13: Implemented auto-switch to workspace in `done()` function. Added `find_workspace_dir()` to locate workspaces at `../ws-{bug_id}`. Modified `done()` to switch directories, run the full workflow (snapshot, describe, link, sync), then return to original directory.",
  "bug_id": "amg2",
  "priority": "high",
  "status": "draft",
  "title": "docket done should auto-switch to workspace if run from trunk"
}
