{
  "body": "## Goal\n\nProvide CLI commands to modify existing bugs without touching jsonl files directly.\n\n## Acceptance Criteria\n\n- [x] `docket new --body <file>` or `--body -` to create bug with full body\n- [x] `docket update <id> --body <file>` to update bug body from file\n- [x] `docket update <id> --body -` to update bug body from stdin\n- [x] `docket update <id> --title <new-title>` to change title\n- [x] `docket update <id> --priority <level>` to change priority\n- [ ] Consider `docket edit <id>` opening $EDITOR (already filed as nzxx)\n\n## Context\n\nCurrently to modify a bug's body after creation, you have to:\n1. Manually construct JSON event\n2. Append to the .jsonl file\n\nThis is error-prone and unfriendly. We need proper CLI commands.\n\nPossible approaches:\n- `docket update` subcommand with flags\n- `docket new` accepting `--body` for full body at creation\n- Pipe-friendly: `cat body.md | docket update <id> --body -`\n\n## Log\n\n- 2026-01-13: Implemented all acceptance criteria except `docket edit` (separate bug nzxx). Added `--body` flag to `docket new`, created new `docket update` command with `--title`, `--body`, and `--priority` flags. Also added `PriorityChanged` event type to support priority updates via event sourcing.",
  "bug_id": "nnpp",
  "priority": "medium",
  "status": "draft",
  "title": "Add commands to modify existing bugs (set body, update fields)"
}
